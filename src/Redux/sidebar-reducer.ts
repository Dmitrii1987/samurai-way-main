
let initialState = {
    friends: [
        { id: 1, name: 'Sasha' },
        { id: 2, name: 'Andrew' },
        { id: 3, name: 'Michael' },
    ]
}

const sidebarReducer = (state=initialState,action:any) => {
    return state
}

export default sidebarReducer